man ls

ls -a
# all (creo que incluso incluye ficheros no visibles).

ls -l
# formato de lista extendida.

ls -d
# Lista los directorios, mas no su contenido.

ls -h
# Formato para lectura humana.

ls -i
# imprime el numero de indice (inodo) de cada archivo/fichero.

ls -R
# Lista los subdirectorios recursivamente.

ls -1
#Lista cada archivo/fichero por linea.

ls -F
# Une uno de los indicadores (*/=>@|) a las entradas.

ls --color
# Colorea la salida